public class Snake {

  // Game loop
  public static void main(String[] args) {
    /*
    model = new Model();
    while(!model.gameOver) {
      input.PollDeviceStates();
      controller.Update(model, input);
      view.Render(model);
    }
     */
  }

}